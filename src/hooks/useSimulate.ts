// import { useState } from 'react';

// const useSimulate = (currentAm: number) => {
//   const [newPrice, setNewPrice] = useState<number>(0);
//   const [currentAmount, setCurrentAmount] = useState<number>(currentAm);

//   const randomFactor = Math.random() * 10 - 5;
//   const newAmount = currentAmount * (1 + randomFactor / 100);
//   setNewPrice(() => newAmount);

//   return newPrice;
// };

// export default useSimulate;
